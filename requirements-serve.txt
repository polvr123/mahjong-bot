# Runtime dependencies for the demo server only (no training extras).
# torch is installed separately in the Dockerfile from the CPU-only index.
mahjong==2.0.0
numpy>=2.0
gymnasium==1.3.0
stable-baselines3==2.9.0
sb3-contrib==2.9.0
fastapi==0.141.1
uvicorn==0.53.0
pydantic>=2.0
